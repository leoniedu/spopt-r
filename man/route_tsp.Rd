% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/route_tsp.R
\name{route_tsp}
\alias{route_tsp}
\title{Traveling Salesman Problem (TSP)}
\usage{
route_tsp(
  locations,
  depot = 1L,
  start = NULL,
  end = NULL,
  cost_matrix = NULL,
  distance_metric = "euclidean",
  method = "2-opt",
  earliest = NULL,
  latest = NULL,
  service_time = NULL
)
}
\arguments{
\item{locations}{An sf object representing locations to visit.}

\item{depot}{Integer. Backward-compatible alias for \code{start} when \code{start} is
not supplied. Defaults to 1.}

\item{start}{Integer. Row index of the route start in \code{locations} (1-based).
If omitted, \code{depot} is used.}

\item{end}{Integer or NULL. Row index of the route end in \code{locations} (1-based).
If omitted, defaults to \code{start} for a closed route. Set to NULL explicitly
for an open route that may end at any stop.}

\item{cost_matrix}{Optional. Pre-computed square distance/cost matrix (n x n).
If NULL, computed from \code{locations} using \code{distance_metric}.}

\item{distance_metric}{Distance metric when computing from geometry:
"euclidean" (default) or "manhattan".}

\item{method}{Algorithm: "2-opt" (default, nearest-neighbor + local search)
or "nn" (nearest-neighbor only).}

\item{earliest}{Optional earliest arrival/service times. Supply either a
numeric vector of length \code{nrow(locations)} or a column name in \code{locations}.}

\item{latest}{Optional latest arrival/service times. Must be supplied
together with \code{earliest}.}

\item{service_time}{Optional service duration at each stop. Supply either a
numeric vector of length \code{nrow(locations)} or a column name in \code{locations}.}
}
\value{
An sf object (the input \code{locations}) with added columns:

\itemize{
\item \code{.visit_order}: Visit sequence (1 = route start)
\item \code{.tour_position}: Position in the returned tour/path
\item \code{.arrival_time}: Arrival/service start time at each visited stop
\item \code{.departure_time}: Departure time after service
}
Metadata is stored in the "spopt" attribute, including \code{total_cost},
\code{nn_cost}, \code{improvement_pct}, \code{tour}, \code{start}, \code{end}, \code{route_type},
and \code{solve_time}.
}
\description{
Solves fixed-start routing problems over an \code{sf} layer. By default the route
is closed (start and end at the same location), but open routes and fixed
start/end paths are also supported. Optional time windows can be supplied in
the same units as the travel-time matrix.
}
\details{
Supported route variants:
\enumerate{
\item \strong{Closed tour}: \verb{start = 1, end = 1} (default)
\item \strong{Open route}: \verb{start = 1, end = NULL}
\item \strong{Fixed path}: \verb{start = 1, end = 5}
}

Time windows use the same units as \code{cost_matrix}. When windows are supplied,
the solver constructs a feasible route and only accepts local-search moves
that preserve feasibility.
}
\seealso{
\code{\link[=route_vrp]{route_vrp()}} for multi-vehicle routing, \code{\link[=distance_matrix]{distance_matrix()}} for
computing cost matrices
}
