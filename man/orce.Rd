% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/locate_orce.R
\name{orce}
\alias{orce}
\title{Warehouse Location Problem (ORCE)}
\usage{
orce(
  demand,
  facilities,
  weight_col,
  cost_matrix,
  facility_cost_col,
  worker_cost,
  worker_capacity,
  max_workers_col,
  min_workers = 1L,
  verbose = FALSE
)
}
\arguments{
\item{demand}{An sf object representing demand points.}

\item{facilities}{An sf object representing candidate facility locations.}

\item{weight_col}{Character. Column name in \code{demand} containing demand
weights (e.g., collection days, workload).}

\item{cost_matrix}{Numeric matrix (demand x facilities). Pre-computed
transport cost from each demand point to each facility.}

\item{facility_cost_col}{Character. Column name in \code{facilities} containing
the fixed cost to open each facility.}

\item{worker_cost}{Numeric scalar. Cost per worker (salary, training, etc.).}

\item{worker_capacity}{Numeric scalar. Maximum demand units a single worker
can handle.}

\item{max_workers_col}{Character. Column name in \code{facilities} containing the
maximum number of workers each facility can employ.}

\item{min_workers}{Integer scalar. Minimum number of workers at each open
facility. Default is 1.}

\item{verbose}{Logical. Print problem dimensions before solving. Default
is FALSE.}
}
\value{
A list with two sf objects:
\itemize{
\item \verb{$demand}: Original demand sf with \code{.facility} column (1-based
index of the assigned facility)
\item \verb{$facilities}: Original facilities sf with \code{.selected} (logical),
\code{.n_assigned} (integer), \code{.workers} (integer), and \code{.utilization}
(numeric) columns
}
Metadata is stored in the "spopt" attribute, including:
\itemize{
\item \code{objective}: Total cost (transport + facility + worker)
\item \code{transport_cost}: Transport cost component
\item \code{facility_cost}: Facility opening cost component
\item \code{worker_cost_total}: Worker cost component
\item \code{n_selected}: Number of open facilities
\item \code{solve_time}: Solver runtime in seconds
}
}
\description{
Solves the warehouse location problem: minimize total transport costs,
facility opening costs, and worker costs. The number of facilities to open
and the number of workers per facility are determined by the optimizer.
}
\details{
The ORCE model extends the uncapacitated facility location problem by adding
worker allocation as integer decision variables. The solver simultaneously
decides which facilities to open, how to assign demand, and how many workers
to place at each facility.

The formulation minimizes:
\deqn{\sum_{ij} c_{ij} x_{ij} + \sum_j f_j y_j + \sum_j w_{cost} \cdot w_j}

Subject to assignment, linking, capacity, and worker bound constraints. See
the package vignette for full details.

The \code{cost_matrix} should contain pre-computed transport costs. Users can
build this from distance and duration data using a helper, e.g.:
\code{cost = distance_km / kml * fuel_cost + duration_hours * hourly_cost}
}
\examples{
\dontrun{
library(sf)

demand <- st_as_sf(data.frame(
  x = runif(30), y = runif(30), workload = rpois(30, 20)
), coords = c("x", "y"))

facilities <- st_as_sf(data.frame(
  x = runif(10), y = runif(10),
  fixed_cost = rep(500, 10),
  max_workers = rep(5L, 10)
), coords = c("x", "y"))

cost <- distance_matrix(demand, facilities)

result <- orce(demand, facilities,
  weight_col = "workload", cost_matrix = cost,
  facility_cost_col = "fixed_cost", worker_cost = 1000,
  worker_capacity = 100, max_workers_col = "max_workers"
)

# Which facilities are open?
result$facilities[result$facilities$.selected, ]

# Cost breakdown
attr(result, "spopt")
}

}
\references{
Leon, E. et al. (2024). orce: Optimization of Statistical Data Collection
Networks. R package. \url{https://github.com/orce-ibge/orce}
}
