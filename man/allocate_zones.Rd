% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/locate_allocate_zones.R
\name{allocate_zones}
\alias{allocate_zones}
\title{Allocate Zones}
\usage{
allocate_zones(
  zones,
  max_distance,
  method = c("nearest", "p_median", "cflp"),
  weight_col = NULL,
  capacity = NULL,
  partition_col = NULL,
  cost_matrix = NULL,
  distance_metric = "euclidean",
  sequence = FALSE,
  verbose = FALSE
)
}
\arguments{
\item{zones}{An sf object where each feature is both a demand point and
a candidate center.}

\item{max_distance}{Numeric. Maximum allowable distance from a zone to its
assigned center.}

\item{method}{Assignment method after K discovery:
\describe{
\item{\code{"nearest"}}{Assign each zone to the nearest selected center.
Fastest. No weights or capacity.}
\item{\code{"p_median"}}{Solve a p-median MILP to minimize total weighted
distance. Uses \code{weight_col} if provided.}
\item{\code{"cflp"}}{Solve a capacitated facility location MILP. Requires
\code{capacity}. Increments K beyond the LSCP minimum if needed.}
}}

\item{weight_col}{Optional character. Column name in \code{zones} containing
demand weights (e.g., population, workload). If NULL, all weights are 1.
Used by \code{"p_median"} and \code{"cflp"} methods.}

\item{capacity}{Optional numeric. Maximum total weight assignable to a
single center. Only used when \code{method = "cflp"}. If \code{weight_col} is NULL,
this is the maximum number of zones per center.}

\item{partition_col}{Optional character. Column name in \code{zones} that defines
independent partitions. Each partition is solved separately.}

\item{cost_matrix}{Optional. Pre-computed square distance matrix (n x n).}

\item{distance_metric}{Distance metric: "euclidean" (default) or "manhattan".
Ignored if \code{cost_matrix} is provided.}

\item{sequence}{Logical. If TRUE, order zones and tracts via TSP for
geographic sequencing. Adds \code{.zone_order} (visit order of zones) and
\code{.tract_order} (visit order of tracts within each zone) columns.
Inter-zone distances use minimum pairwise distance between zone members.}

\item{verbose}{Logical. Print progress messages.}
}
\value{
A list with a single sf object:
\itemize{
\item \verb{$zones}: Original sf with added columns:
\itemize{
\item \code{.center}: Index of the assigned center zone
\item \code{.is_center}: TRUE if this zone was selected as a center
\item \code{.distance}: Distance to assigned center
\item \code{.component}: Partition ID (sequential integer)
}
When \code{method = "cflp"}, a \code{.split} column is added (TRUE if demand
is split across centers). When \code{sequence = TRUE}, \code{.zone_order} and
\code{.tract_order} columns give TSP-based visit ordering.
}
Metadata is stored in the "spopt" attribute.
}
\description{
Partitions geographic units into zones, each with a center, such that no
unit is farther than \code{max_distance} from its center. Automatically finds
the minimum number of centers needed via LSCP, then assigns units using
one of three methods.
}
\details{
All methods use LSCP (Location Set Covering Problem) to find the minimum
number of centers K* such that every zone has a center within \code{max_distance}.
They differ in how assignments are made:

\describe{
\item{\code{"nearest"}}{Greedy nearest-center assignment. One MILP solve.
Equivalent to \code{"p_median"} when all weights are equal.}
\item{\code{"p_median"}}{Lexicographic: minimize K (LSCP), then minimize
total weighted distance (p-median). Two MILP solves per partition.}
\item{\code{"cflp"}}{Like \code{"p_median"} but with capacity constraints.
If LSCP's K* is infeasible under capacity, K is incremented until
a feasible solution is found. Two or more MILP solves per partition.}
}
}
\examples{
\dontrun{
library(sf)

zones <- st_as_sf(data.frame(
  x = c(0, 0.1, 0.2, 10, 10.1, 10.2),
  y = c(0, 0.1, 0.2, 0, 0.1, 0.2),
  pop = rep(100, 6)
), coords = c("x", "y"))

# Fast: LSCP + nearest
allocate_zones(zones, max_distance = 1)

# Optimal weighted: LSCP + p-median
allocate_zones(zones, max_distance = 1, method = "p_median", weight_col = "pop")

# Capacitated: max 2 zones per center
allocate_zones(zones, max_distance = 1, method = "cflp", capacity = 2)
}

}
\seealso{
\code{\link[=p_median]{p_median()}} and \code{\link[=cflp]{cflp()}} for standard facility location with
separate demand/facilities and user-specified K
}
